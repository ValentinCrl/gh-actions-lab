name: Workflow d'initiation
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: saluer-le-monde 
        run: |
          echo "Bonjour depuis GitHub Actions !"
          # Add your actual script commands here
      
      - name: afficher-la-date 
        run: |
          echo "La date actuelle est : $(date)"
          # Add your actual script commands here